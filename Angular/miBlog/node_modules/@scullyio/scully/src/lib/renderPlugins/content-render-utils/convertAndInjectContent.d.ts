import { JSDOM } from 'jsdom';
import { HandledRoute } from '../../..';
export declare function convertAndInjectContent(dom: JSDOM, anyContent: string, type: string, route: HandledRoute): Promise<JSDOM>;
