import { JSDOM } from 'jsdom';
import { ContentTextRoute } from '../routerPlugins/handledRoute.interface';
export declare function contentTextRenderPlugin(dom: JSDOM, route: ContentTextRoute): Promise<JSDOM>;
