"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deepClone = void 0;
/**
 * Helper to deep-clone any JS variable.
 * @param original
 */
exports.deepClone = (original) => {
    /**  if not array or object or is null return self */
    if (typeof original !== 'object' || original === null) {
        return original;
    }
    if (original instanceof Date) {
        return new Date(original.getTime());
    }
    /** using a single var for both return values gives some air to TS type system. */
    let clone;
    if (original instanceof Array) {
        const l = original.length;
        clone = [];
        for (let i = 0; i < l; i++) {
            clone[i] = exports.deepClone(original[i]);
        }
        return clone;
    }
    clone = {};
    for (const i in original) {
        if (original.hasOwnProperty(i)) {
            clone[i] = exports.deepClone(original[i]);
        }
    }
    return clone;
};
//# sourceMappingURL=deepClone.js.map