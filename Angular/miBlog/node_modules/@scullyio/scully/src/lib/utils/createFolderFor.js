"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createFolderFor = void 0;
const fs_1 = require("fs");
const path_1 = require("path");
function createFolderFor(file) {
    /** drop the /xxx.html part of the route */
    const dirPath = path_1.dirname(file);
    try {
        // @ts-ignore
        fs_1.mkdirSync(dirPath, { recursive: true });
    }
    catch (err) {
        if (err.code !== 'EEXIST') {
            throw err;
        }
    }
}
exports.createFolderFor = createFolderFor;
//# sourceMappingURL=createFolderFor.js.map