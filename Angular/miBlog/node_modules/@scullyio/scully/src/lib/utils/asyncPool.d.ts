/**
 * takes an array, and runs **MaxParalellTasks** in paralell until all tasks are node
 * @param MaxParalellTasks
 * @param array
 * @param taskFn
 */
export declare function asyncPool<T>(MaxParalellTasks: number, array: T[], taskFn: (x: T) => Promise<T>): Promise<T[]>;
