import { ScullyConfig } from './interfacesandenums';
export declare function validateConfig(config: ScullyConfig): Promise<ScullyConfig>;
export declare function checkFolderExists(folder: string): boolean;
